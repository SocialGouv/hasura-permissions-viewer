{
  "name": "@socialgouv/hasura-permissions-viewer",
  "description": "convert hasura permissions definition to readable HTML",
  "version": "1.0.1",
  "main": "src/index.js",
  "license": "Apache-2.0",
  "files": [
    "src"
  ],
  "devDependencies": {
    "@socialgouv/eslint-config-recommended": "1.7.0",
    "eslint": "6.8.0",
    "jest-cli": "26.0.1",
    "prettier": "2.0.5"
  },
  "eslintConfig": {
    "extends": [
      "@socialgouv/eslint-config-recommended"
    ]
  },
  "scripts": {
    "lint": "eslint src/**/*.js",
    "test": "jest"
  },
  "release": {
    "branches": [
      "master"
    ],
    "plugins": [
      "@semantic-release/commit-analyzer",
      "@semantic-release/release-notes-generator",
      "@semantic-release/npm",
      "@semantic-release/github"
    ]
  }
}
